<UserControl x:Class="Biblioteca.Cliente.WPF.Views.ReservasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550"
             d:DesignWidth="900"
             Loaded="UserControl_Loaded">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">

            <Button x:Name="btnAtualizarReservas" Content="Atualizar" Width="100" Height="35" Click="btnAtualizarReservas_Click"/>

            <Button x:Name="btnRegistarReserva" Content="Registar" Width="100" Height="35" Margin="10,0,0,0" Click="btnRegistarReserva_Click"/>

            <Button x:Name="btnCancelarReserva" Content="Cancelar" Width="100" Height="35" Margin="10,0,0,0" Click="btnCancelarReserva_Click"/>

            <Button x:Name="btnEliminarReserva" Content="Eliminar" Width="100" Height="35" Margin="10,0,0,0" Click="btnEliminarReserva_Click"/>

        </StackPanel>

        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Utilizador:" VerticalAlignment="Center" Margin="5"/>

            <ComboBox x:Name="cbUtilizadores" Grid.Column="1" Height="30" Margin="5" DisplayMemberPath="Nome" SelectedValuePath="IdUtilizador"/>

            <TextBlock Grid.Column="2" Text="Livro:" VerticalAlignment="Center" Margin="5"/>

            <ComboBox x:Name="cbLivros" Grid.Column="3" Height="30" Margin="5" DisplayMemberPath="Titulo" SelectedValuePath="IdLivro"/>

        </Grid>

        <DataGrid x:Name="dgReservas" Grid.Row="4" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="dgReservas_SelectionChanged">

            <DataGrid.Columns>

                <DataGridTextColumn Header="ID" Binding="{Binding IdReserva}" Width="60"/>

                <DataGridTextColumn Header="Utilizador" Binding="{Binding Utilizador}" Width="*"/>

                <DataGridTextColumn Header="Livro" Binding="{Binding Livro}" Width="*"/>

                <DataGridTextColumn Header="Data da reserva" Binding="{Binding DataReserva, StringFormat=dd/MM/yyyy}" Width="130"/>

                <DataGridTextColumn Header="Disponível desde" Binding="{Binding DataDisponivel, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>

                <DataGridTextColumn Header="Ordem" Binding="{Binding Ordem}" Width="80"/>

                <DataGridCheckBoxColumn Header="Ativa" Binding="{Binding Ativa}" Width="80"/>

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>