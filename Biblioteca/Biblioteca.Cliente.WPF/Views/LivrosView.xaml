<UserControl x:Class="Biblioteca.Cliente.WPF.Views.LivrosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550"
             d:DesignWidth="900"
             Background="#F5F5F5"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestão de Livros" FontSize="26" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" Padding="15" CornerRadius="6" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button x:Name="btnAtualizarLivros" Content="Atualizar" Width="180" Height="35" Click="btnAtualizarLivros_Click"/>
                    <Button x:Name="btnGuardarLivro" Content="Guardar" Width="100" Height="35" Margin="10,0,0,0" Click="btnGuardarLivro_Click"/>
                    <Button x:Name="btnAlterarLivro" Content="Alterar" Width="100" Height="35" Margin="10,0,0,0" Click="btnAlterarLivro_Click"/>
                    <Button x:Name="btnEliminarLivro" Content="Eliminar" Width="100" Height="35" Margin="10,0,0,0" Click="btnEliminarLivro_Click"/>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Título:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtTitulo" Grid.Row="0" Grid.Column="1" Height="30" Margin="0,0,20,10"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Autor:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtAutor" Grid.Row="0" Grid.Column="3" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Editora:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtEditora" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,0,20,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Ano de publicação:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtAnoPublicacao" Grid.Row="1" Grid.Column="3" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Género:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtGenero" Grid.Row="2" Grid.Column="1" Height="30" Margin="0,0,20,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Exemplares disponíveis:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtExemplaresDisponiveis" Grid.Row="2" Grid.Column="3" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Categoria:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox x:Name="cmbCategoria" Grid.Row="3" Grid.Column="1" Height="30" Margin="0,0,20,0" DisplayMemberPath="Nome" SelectedValuePath="IdCategoria"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" Padding="15" CornerRadius="6" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Pesquisar:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <TextBox x:Name="txtPesquisaLivro" Grid.Row="0" Grid.Column="1" Height="30" Margin="0,0,20,10"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Categoria:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                <ComboBox x:Name="cmbFiltroCategoria" Grid.Row="0" Grid.Column="3" Height="30" Margin="0,0,0,10" DisplayMemberPath="Nome" SelectedValuePath="IdCategoria"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Estado:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbEstadoLivro" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,0,20,0"/>

                <StackPanel Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="btnPesquisarLivro" Content="Pesquisar" Width="100" Height="30" Margin="0,0,10,0" Click="btnPesquisarLivro_Click"/>
                    <Button x:Name="btnLimparPesquisa" Content="Limpar" Width="100" Height="30" Click="btnLimparPesquisa_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="dgLivros"
          Grid.Row="3"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          SelectionMode="Single"
          HorizontalScrollBarVisibility="Auto"
          SelectionChanged="dgLivros_SelectionChanged">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdLivro}" Width="55"/>

                <DataGridTextColumn Header="Título" Binding="{Binding Titulo}" Width="2*"/>

                <DataGridTextColumn Header="Autor" Binding="{Binding Autor}" Width="1.5*"/>

                <DataGridTextColumn Header="Editora" Binding="{Binding Editora}" Width="1.3*"/>

                <DataGridTextColumn Header="Ano" Binding="{Binding AnoPublicacao}" Width="75"/>

                <DataGridTextColumn Header="Género" Binding="{Binding Genero}" Width="*"/>

                <DataGridTextColumn Header="Exemplares" Binding="{Binding ExemplaresDisponiveis}" Width="90"/>

                <DataGridTextColumn Header="Categoria" Binding="{Binding Categoria}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>