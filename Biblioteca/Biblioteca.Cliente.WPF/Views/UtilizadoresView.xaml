<UserControl x:Class="Biblioteca.Cliente.WPF.Views.UtilizadoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550"
             d:DesignWidth="900"
             Background="#F5F5F5"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestão de Utilizadores" FontSize="26" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" Padding="15" CornerRadius="6" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button x:Name="btnAtualizarUtilizadores" Content="Atualizar" Width="100" Height="35" Click="btnAtualizarUtilizadores_Click"/>
                    <Button x:Name="btnGuardarUtilizador" Content="Guardar" Width="100" Height="35" Margin="10,0,0,0" Click="btnGuardarUtilizador_Click"/>
                    <Button x:Name="btnAlterarUtilizador" Content="Alterar" Width="100" Height="35" Margin="10,0,0,0" Click="btnAlterarUtilizador_Click"/>
                    <Button x:Name="btnEliminarUtilizador" Content="Eliminar" Width="100" Height="35" Margin="10,0,0,0" Click="btnEliminarUtilizador_Click"/>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Nome:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtNome" Grid.Row="0" Grid.Column="1" Height="30" Margin="0,0,20,10"/>

                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Contacto:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtContacto" Grid.Row="0" Grid.Column="3" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtEmail" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,0,20,10"/>

                    <TextBlock Grid.Row="1" Grid.Column="2" Text="Tipo de utilizador:" VerticalAlignment="Center" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtTipoUtilizador" Grid.Row="1" Grid.Column="3" Height="30" Margin="0,0,0,10"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Limite de empréstimos:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtLimiteEmprestimos" Grid.Row="2" Grid.Column="1" Height="30" Margin="0,0,20,0"/>

                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Atrasos:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtAtrasos" Grid.Row="2" Grid.Column="3" Height="30"/>
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" Padding="15" CornerRadius="6" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Pesquisar:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtPesquisaUtilizador" Grid.Column="1" Height="30" Margin="0,0,20,0"/>

                <Button x:Name="btnPesquisarUtilizador" Grid.Column="2" Content="Pesquisar" Height="30" Margin="0,0,10,0" Click="btnPesquisarUtilizador_Click"/>
                <Button x:Name="btnLimparPesquisa" Grid.Column="3" Content="Limpar" Height="30" Click="btnLimparPesquisa_Click"/>
            </Grid>
        </Border>

        <DataGrid x:Name="dgUtilizadores"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HorizontalScrollBarVisibility="Auto"
                  SelectionChanged="dgUtilizadores_SelectionChanged">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdUtilizador}" Width="60"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Contacto" Binding="{Binding Contacto}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoUtilizador}" Width="120"/>
                <DataGridTextColumn Header="Limite" Binding="{Binding LimiteEmprestimos}" Width="80"/>
                <DataGridTextColumn Header="Atrasos" Binding="{Binding Atrasos}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>