<UserControl x:Class="Biblioteca.Cliente.WPF.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="800"
             Background="#F5F5F5"
             Loaded="UserControl_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gestão de Categorias" FontSize="26" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" Padding="15" CornerRadius="6" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                    <Button x:Name="btnAtualizarCategorias" Content="Atualizar" Width="180" Height="35" Click="btnAtualizarCategorias_Click"/>
                    <Button x:Name="btnGuardarCategoria" Content="Guardar" Width="100" Height="35" Margin="10,0,0,0" Click="btnGuardarCategoria_Click"/>
                    <Button x:Name="btnAlterarCategoria" Content="Alterar" Width="100" Height="35" Margin="10,0,0,0" Click="btnAlterarCategoria_Click"/>
                    <Button x:Name="btnEliminarCategoria" Content="Eliminar" Width="100" Height="35" Margin="10,0,0,0" Click="btnEliminarCategoria_Click"/>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Nome:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBox x:Name="txtNome" Grid.Column="1" Height="30"/>

                    <TextBlock Grid.Column="2" Text="Descrição:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                    <TextBox x:Name="txtDescricao" Grid.Column="3" Height="30"/>
                </Grid>
            </Grid>
        </Border>

        <DataGrid x:Name="dgCategorias"
                  Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HorizontalScrollBarVisibility="Auto"
                  SelectionChanged="dgCategorias_SelectionChanged"/>
    </Grid>
</UserControl>